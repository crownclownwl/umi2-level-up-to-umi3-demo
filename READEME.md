#记得先要执行如下命令：
```shell
    #设置npm排除 purpeer 设置npm 和 yarn 跳过 安装 chromium 浏览器支持的 pupper 依赖
    $ npm config set puppeteer_skip_chromium_download true
    $ yarn config set puppeteer_skip_chromium_download true
```